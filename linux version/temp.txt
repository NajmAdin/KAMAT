CREATE TABLE users (
   userid INT PRIMARY KEY,
   username VARCHAR(40),
   password VARCHAR(255),
   email VARCHAR(255))


INSERT INTO users VALUES ( 3, 'najm', 'password', 'email')

SELECT * FROM users WHERE userid = 1
DELETE FROM users WHERE userid =1

SELECT * FROM users ;